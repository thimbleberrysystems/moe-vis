# Copy to env.sh and adjust paths for your machine, then: source env.sh
export GOROOT=$HOME/sdk/go1.26.4            # path to your Go 1.26+ install
export PATH=$GOROOT/bin:$PWD/venv/bin:$PATH # Go + the project's python venv
export CC=gcc CXX=g++
